import test from "ava";

test("renders a link from a link field", (t) => {});

test("uses link resolver provided via context", (t) => {});

test("uses link resolver provided via props", (t) => {});

test("if given a link field value, use its target", (t) => {});

test("if given a href, only use the prop target", (t) => {});

test('if target is blank, use rel="noopener noreferrer" by default', (t) => {});

test("allow overriding default rel", (t) => {});

test("if URL is internal, render InternalComponent", (t) => {});

test("if URL is external, render ExternalComponent", (t) => {});
